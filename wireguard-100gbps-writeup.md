# Scaling WireGuard with a multi-tunnel fabric — measured results

> **Status: GOAL MET — 103.2 Gbps measured** (live runs **2026-06-19/20**, two
> `i8ge.48xlarge` Spot nodes in `us-west-2d`, generated by `report/` from `results/`). Nothing
> here is fabricated; every value traces to a `datapoint.json` with attribution from counter/CPU
> deltas on **both** nodes.
>
> **A multi-tunnel WireGuard fabric carries ≥100 Gbps of encrypted throughput between two
> `i8ge.48xlarge` nodes — and ~148 Gbps aggregate when both directions run.** Unidirectional,
> node-split placement crossed 100 Gbps at **N=40 → 103.2 Gbps** (113 on a warmer instance) and
> held through N=64. **Bidirectional** (paired opposing flows, symmetric tuning) peaked at
> **148.4 Gbps aggregate wire** (N=32: 69.5 A→B + 78.9 B→A), tapering to 127 by N=64. In every
> case **no AWS allowance fired** — the binding wall is aggregate ChaCha20 CPU across all 192
> cores, never the network (raw ENA hit 208 Gbps with no WG; ENA meters in/out *separately* at
> ~180 each, so there is no single aggregate cap to hit). The decisive unlock was **NUMA-aware
> placement**: spread the work across both memory controllers, don't hoard the NIC-local node.
>
> **Per-packet offloads are a dead end on ENA:** GSO/GRO are already on; hardware UDP
> segmentation (`tx-udp-segmentation`) is `[fixed]` off and un-toggleable, and enlarging RX
> rings (1024→8192) did not help (slightly hurt) — when you're CPU-bound, more buffering isn't
> throughput. Placement, not per-byte cost, was the lever.
>
> **Headline:** the multi-tunnel fabric scales near-linearly to ~50 Gbps (N=8), then the
> *naive* curve plateaus at **~60 Gbps** — bound by **aggregate receive-side CPU** (NAPI +
> ChaCha20 decrypt kworkers + ksoftirqd + memory-mgmt churn + the userspace reader), *not* by
> any AWS allowance and *not* by the ~180 Gbps network wall (raw ENA hit 208 Gbps with no WG).
>
> **CPU placement, not a hard wall — and the winning placement is COUNTERINTUITIVE.** A
> sequence of placement experiments climbed the curve: distinct-core pinning 60→77; then
> putting the **whole receive pipeline on the NIC-local NUMA node** → **89.5 Gbps**. But the
> best result came from the opposite of "keep it all NIC-local": **splitting the tunnels
> across BOTH NUMA nodes → 95.3 Gbps at N=32** (project best, within 5% of 100 Gbps), at
> **2.56 Gbps/core-equiv vs 1.28 for NIC-local-only** — efficiency *doubled*, receiver no
> longer pegged (79% peak core). Reason: cramming all 62 busy core-equivalents onto node 1's
> 96 cores + one memory controller was **memory/contention bound**; spreading across both
> nodes' memory subsystems relieves it, and the cross-node cost is cheap (see below). Using all
> 192 cores beats hoarding the NIC's node.
>
> **The 90% remote-memory "penalty" is interconnect saturation, not per-access cost.** A
> STREAM thread-sweep showed remote BW plateaus at ~69 GB/s by 24 threads (flat to 96) — a
> fixed shared ceiling — while a pointer-chase put single-access remote latency at just **2.2×**
> local (307 vs 141 ns), matching the firmware SLIT. So a *moderate* cross-node hand-off
> (per-flow ~3 Gbps, far under 69 GB/s) is cheap; only bulk-payload streaming hits the wall.
> This is exactly why the node-split wins.

## Thesis (as stated going in)

A single WireGuard tunnel can't reach 100 Gbps on an `i8ge.48xlarge`, because every
bottleneck is keyed on the **flow** — and a WireGuard interface is one UDP 5-tuple:

1. **Per-flow bandwidth cap** — one 5-tuple is capped (5 / 10 / 25 Gbps).
2. **Single RX queue** — one 5-tuple RSS-hashes to one ENA queue → one core's softirq.
3. **Per-peer crypto ordering** — one tunnel's ChaCha20-Poly1305 is one serial pipeline.

N tunnels = N flows = N RX queues = N crypto pipelines, so throughput should scale
near-linearly with N until a **global** ceiling binds. The prediction was that the global
ceiling would be the ~180 Gbps VPC allowance. **The measured global ceiling turned out to be
aggregate receive-side CPU/crypto at ~60 Gbps instead** — see Findings.

## Rig

- 2 × `i8ge.48xlarge` — Graviton4, **192 vCPU**, 1536 GiB DDR5-5600, single network card,
  16 × 6.8 TB local Nitro NVMe instance store. Spot, `us-west-2d`, cluster placement group.
- Ubuntu 24.04 arm64, **kernel 6.17.0-1017-aws**, in-tree WireGuard. ChaCha20-Poly1305.
- Jumbo: **9001** MTU on the ENA (`ens146`), **8921** on the 32 wg interfaces.

## Measured ceilings (baselines)

| Ceiling | Tool | Measured | Notes |
|---|---|---|---|
| Raw ENA throughput (no WG, 32 streams) | `measure-baseline.sh` | **208.1 Gbps** | above the nominal 180 — burst allowance over a 30 s window |
| Raw ENA packet rate | `measure-baseline.sh` | **2.90 M pps** | at jumbo MTU |
| NVMe striped sequential **read** (16 dev) | `measure-nvme.sh` | **52.3 GB/s** | instance-store ceiling |
| NVMe striped sequential **write** (16 dev) | `measure-nvme.sh` | **25.9 GB/s** | the sink-side storage limiter |
| Memory bandwidth (STREAM Triad, 192c) | `measure-membw.sh` | **4885 GB/s** | enormous headroom; ~90 GB/s needed at 180 Gbps wire — never binds |
| Per-core ChaCha20 throughput | derived | **~7–8 Gbps/tunnel** in the linear region (see sweep) | each added tunnel ≈ +7 Gbps up to N=8 |

The raw ENA path reaching **208 Gbps with no WireGuard** is itself notable — it proves the
network fabric is *not* the constraint at 60 Gbps. The bottleneck the sweep hits is above WG.

## The sweep — aggregate Gbps vs tunnel count

`placement` = cluster placement group. `ena_express` = ENA Express UDP enabled on both ENIs.
DUR=15 s per datapoint; attribution captured on both nodes.

### `placement`

| N | Gbps | sender PPS | max core util (A/B) | softirq top-core (A) | binding limit (measured) |
|---|------|-----------|---------------------|----------------------|--------------------------|
| 1 | 7.9 | 111.8 k | 10.6 / 26.0 | 0.78 | single RX queue (need more tunnels) |
| 2 | 15.8 | 223.6 k | 12.1 / — | 0.38 | linear region (unbound) |
| 4 | 30.9 | 437.6 k | 15.5 / 65.6 | 0.20 | linear region (unbound) |
| 8 | 54.7 | 777.9 k | 19.6 / 81.1 | 0.12 | linear region (unbound) |
| 12 | 56.2 | 793.6 k | 17.5 / — | 0.08 | CPU / crypto |
| 16 | 57.3 | 813.6 k | 14.0 / 99.3 | 0.09 | CPU / crypto |
| 24 | 57.9 | 813.1 k | 12.4 / — | 0.08 | CPU / crypto |
| 32 | 60.0 | 847.6 k | 11.7 / 98.0 | 0.07 | CPU / crypto |

Every datapoint had `bw_out_allowance_exceeded = 0`, `bw_in_allowance_exceeded = 0`,
`pps_allowance_exceeded = 0`, `conntrack_allowance_exceeded = 0` on **both** nodes — i.e.
**no AWS allowance ever fired.** The knee is not the network.

### `ena_express`

| N | Gbps | SRD tx pkts | max core util (A/B) | binding limit (measured) |
|---|------|-------------|---------------------|--------------------------|
| 1 | 7.9 | 12 | — / 43.0 | single RX queue |
| 2 | 15.7 | 11 | — | linear region |
| 4 | 30.5 | 17 | — | linear region |
| 8 | 52.8 | 47 | — / 78.9 | linear region |
| 12 | 54.1 | 21 | — | CPU / crypto |
| 16 | 55.9 | 19 | — | CPU / crypto |
| 24 | 57.6 | 11418 | — | CPU / crypto |
| 32 | 59.7 | 3487 | — / 98.1 | CPU / crypto |

> **SRD honesty note:** ENA Express UDP was enabled and **eligible** packets numbered in the
> millions, but `ena_srd_tx_pkts` stayed tiny (tens to a few thousand) versus eligible — SRD
> ramps adaptively and barely engaged in these 15 s windows. So `ena_express` is **not**
> meaningfully a "traffic rode SRD" result here, and unsurprisingly its curve is
> indistinguishable from `placement`. Reported as measured, not overclaimed.

## Real workload — NVMe→NVMe over nvme-tcp (native multipath)

One nvme-tcp path per tunnel against a single target subsystem (16 namespaces), round-robin
iopolicy, so each I/O command rides a distinct tunnel/flow.

| mode | N | NVMe GB/s | equiv. Gbps | binding limit (measured) |
|---|---|-----------|-------------|--------------------------|
| read | 8 | 6.2 | 49.8 | linear region |
| read | 16 | **8.9** | 71.3 | CPU / crypto |
| read | 32 | 8.4 | 67.1 | CPU / crypto |
| write | 8 | 6.4 | 51.2 | linear region |
| write | 16 | 7.4 | 59.0 | linear region |
| write | 32 | 8.3 | 66.7 | CPU / crypto |

The real storage path **tracks the synthetic sweep** (~50–71 Gbps equivalent, same CPU/crypto
plateau). Peak **8.9 GB/s read** is far below the 52 GB/s local NVMe ceiling and the 25.9 GB/s
write ceiling — so the limiter is the encrypted network/crypto path, exactly as the iperf
sweep showed, not the disks.

### NVMe near:far NUMA placement (run 4)

Given the 90% remote-memory penalty, does it matter whether the target drives are on the
NIC-local node (no hop) or the far node (extra cross-complex DMA)? Measured at N=32, exporting
only node-1 (near), only node-0 (far), or all 16 (balanced):

| target drives | read GB/s | write GB/s |
|---|---|---|
| 8× near (node 1, NIC-local) | **8.3** | 7.4 |
| 8× far (node 0) | 7.5 | 7.9 |
| 16 balanced (both nodes) | 7.2 | 7.8 |

**Storage placement barely matters here** — near vs far differs by ~10% on read, with near
slightly ahead on read and far marginally ahead on write (within run-to-run noise).
The reason: the workload (~9 GB/s ≈ 70 Gbps) is **CPU/network-bound at the same ~70 Gbps
plateau**, far below the 69 GB/s *remote* memory ceiling, so the cross-NUMA hop has ample
headroom and barely bites. The only real signal is **balanced read dropping to 6.9 GB/s** —
spreading across 16 drives (vs 8) added mild contention. Practical takeaway: the near:far drive
balance is **not a useful lever at this throughput**; it would only start to matter if storage
bandwidth approached the ~69 GB/s remote-memory ceiling, and CPU binds long before that. Keep
it simple — NIC-local drives, since "near" is marginally best and never worse.

## Plots

- `throughput.svg` — aggregate Gbps vs N, per mode, with the ~180 Gbps wall drawn for scale.
- `efficiency.svg` — Gbps per busy core vs N.

## Findings

1. **The fabric scales near-linearly up to N≈8**, then plateaus. Placement: 7.9 → 15.7 → 29.1
   → 48.0 Gbps for N = 1, 2, 4, 8 (≈2× per doubling — the thesis's core mechanism is real).
2. **N=1 is single-RX-queue bound** (softirq concentrated on one core: top-core share ~0.78),
   exactly the per-flow serialization the thesis predicts. Adding tunnels relieves it.
3. **The unpinned plateau (~57–60 Gbps) is aggregate receive-side CPU, not the network and
   not one core.** No `bw_*`/`pps`/`conntrack` allowance fired at any N on either node. Run 2's
   per-core instrumentation shows the receiver spending **~57 core-equivalents across ~31
   cores >50% busy** at N=32 — broad, distributed load. The hot threads are exactly the
   encrypted-receive pipeline: `napi/wgN-0` (NAPI poll), `kworker/*-wg-crypt-wgN` (ChaCha20
   decrypt workqueues), `ksoftirqd/*`, and the userspace `iperf3` receivers. The earlier
   single-run reading of "one core at 99%" was one marginally-hotter core atop that wide load,
   not a serial bottleneck.
4. **The receiver costs ~4× the sender.** At N=32: receiver **57 core-equivalents** vs sender
   **15**. Decrypt + receive dominates; the receiver is the binding side, as predicted.
5. **Per-core efficiency degrades with N** (unpinned): 2.53 → 2.13 → 1.91 → 1.31 →
   **1.04 Gbps/core-equiv** for N = 1, 4, 8, 16, 32. Each added tunnel costs progressively
   more CPU per Gbps — softirq/scheduling/cache overhead from spreading the receive path ever
   wider. This diminishing return, not a hard wall, is what flattens the curve.
6. **Pinning the receive workers recovers a large chunk of that loss.** Pinning each tunnel's
   `iperf3` server to a distinct core (`pin-workers.sh`) lifted the curve substantially at
   high N:

   | N | unpinned Gbps | pinned Gbps | Δ |
   |---|---------------|-------------|---|
   | 8 | 48.0 | 42.2 | −12% |
   | 16 | 55.3 | 58.6 | +6% |
   | 24 | 55.8 | 69.2 | +24% |
   | 32 | 59.9 | **77.2** | **+29%** |

   At N=32 this used the **same ~57 core-equivalents** but delivered 77 vs 60 Gbps —
   efficiency **1.04 → 1.35 Gbps/core (+30%)**. Same CPU budget, more throughput: the gain is
   pure cache-locality / reduced task migration. (At low N pinning slightly *hurts* — it
   over-constrains when there is spare capacity to schedule freely.)
7. **100 Gbps was still not reached** (best: 77 Gbps, pinned, N=32) — but the binding
   constraint is decisively **per-core efficiency / CPU placement**, not the ~180 Gbps VPC
   wall (raw ENA hit 208 Gbps with no WG).
8. **ENA Express made no measurable difference**, consistent with SRD barely engaging (tiny
   `ena_srd_tx_pkts` vs millions eligible) and with the ceiling being CPU-bound.
9. **The real NVMe→NVMe workload (run 1) confirmed the synthetic curve** — same shape, same
   ceiling, well under the local storage ceilings.

### Honest caveats

- **Pinning was userspace-only.** `pin-workers.sh` pins the `iperf3` servers; it does **not**
  pin the kernel's wg-crypt kworkers or ksoftirqd (kernel-controlled). The +29% came from
  pinning userspace alone — pinning the crypto path too (via RPS/RFS / XPS / IRQ-to-decrypt
  affinity) is the obvious next lever and might close more of the gap to 100 Gbps.
- **`busy_cores` (cores >90%) under-counts** when work spreads thinly; `busy_core_equiv`
  (core-equivalents) is the metric that actually answers "how many cores' worth of work" and
  is what the findings above rely on.
- **Short windows (DUR=15–20 s).** Steady-state for the sweep, but SRD's adaptive ramp wants
  longer; a multi-minute `ena_express` run might engage SRD materially.
- **Pinned run was placement-mode only** (one A/B). A pinned `ena_express` and a finer N grid
  near the knee would sharpen the picture.

### What would push past 77 Gbps (hypotheses, partly tested)

The lever is per-core receive efficiency, now demonstrated by the pinning result:

- **NUMA-aware placement** — if the instance presents >1 NUMA node, pin the decrypt path and
  workers to the node the ENA NIC is local to, so packets don't cross the interconnect. See
  the dedicated note below; `detect-numa.sh` + `NODE=<n> pin-workers.sh` test this directly.
- **Pin the kernel decrypt path too** — RPS/RFS to steer each tunnel's RX to a fixed core,
  XPS for TX, and align the wg-crypt kworker affinity with the RX core (cache-warm decrypt).
- **Pipeline the stages** (read→encrypt→network→decrypt→write) so a flow's decrypt, TCP
  delivery, and disk write run on different cores rather than contending — see the dedicated
  note below; this is the most promising untested direction.
- The network (208 Gbps) and storage (52 GB/s read) have ample headroom; only the
  encrypted-receive CPU pipeline binds.

### NUMA placement — MEASURED, and the result is counterintuitive

`i8ge.48xlarge` **presents 2 NUMA nodes** (node 0 = cores 0-95, node 1 = cores 96-191), and
the guest *does* expose device affinity: the **ENA NIC is on node 1** (`numa_node=1`), and the
16 instance-store NVMe split 8/8 across both nodes (`detect-numa.sh` → `results/numa.json`).

So I ran the decisive A/B on the receiver at N=8/16/24/32: pin all userspace workers to the
**NIC-local** node (`NODE=1`) vs the **NIC-remote** node (`NODE=0`):

| N | `NODE=1` NIC-local | `NODE=0` NIC-remote |
|---|--------------------|---------------------|
| 8 | 55.9 | 53.3 |
| 16 | 57.3 | 61.4 |
| 24 | 57.0 | 70.8 |
| 32 | **56.3** | **75.7** |

In this configuration the NIC-*remote* node was faster — but **read the confound before
drawing the obvious conclusion.** Instrumentation (N=32): NIC-local pinning smeared work
across **55 cores >50% busy** at 0.97 Gbps/core-equiv; NIC-remote concentrated it in **30
cores** at 1.39 Gbps/core-equiv.

**Confound (important):** `node-setup.sh`, as run for these sweeps, pinned the 32 ENA RX IRQs
to cores **0–31 — all on node 0, the NIC-*remote* node** (its round-robin started at core 0
and was NUMA-blind; the NIC is on node 1). So the RX softirq/NAPI processing was on node 0
the whole time. That means the A/B actually compared:

- **`NODE=0`:** userspace receivers **co-located with the RX-softirq cores** (both node 0) →
  76 Gbps, work concentrated, cache-warm hand-off from softirq to the userspace socket.
- **`NODE=1`:** userspace receivers **split from the RX-softirq cores** (userspace node 1,
  softirq node 0) → 56 Gbps, cross-node hand-off on every packet.

So the honest finding from run 3 was **"co-locate userspace receive with the RX-softirq
cores," not "avoid the NIC-local node"** — the IRQs never actually ran on the NIC-local node,
so the textbook layout was untested.

**Run 4 fixed the IRQ pinning and retested** (IRQs now verified on NIC-local cores 96–191;
softirq/ksoftirqd followed them there). The A/B *flipped*, confirming the textbook layout and
the original NUMA intuition:

| N=32, IRQs NIC-local (node 1) | Gbps | Gbps/core-equiv |
|---|---|---|
| userspace `NODE=1` (all NIC-local) | **89.5** | 1.42 |
| userspace unpinned | 59.8 | 0.99 |
| userspace `NODE=0` (remote from softirq) | 55.4 | 0.93 |

(Re-run values; the all-NIC-local layout reached **89.5 Gbps** — the project's best, with the
receiver at 62.9 busy core-equivalents / 100% peak core. The receive threads at N=32 are
`ksoftirqd` + `wg-crypt` kworkers on node-1 cores 96–191, confirming the whole pipeline ran
NIC-local as intended.)

**Conclusion (measured, not inferred): keep the whole receive pipeline — RX IRQ, NAPI/softirq,
decrypt, and userspace — on the NIC-local node.** This took the fabric to **89.5 Gbps** at
N=32 — within striking distance of 100 Gbps and far above the unpinned ~60. Run 3's "remote
wins" was purely an artifact of NUMA-blind IRQ placement (`node-setup.sh` pinned IRQs to cores
0–31 = the remote node); once corrected, NIC-local co-location is best, as hardware locality
predicts.

**Per-NUMA memory bandwidth** (run 4, `measure-membw-numa.sh`): **local ≈ 381 GB/s, remote ≈
69 GB/s — a 90% / 5.5× penalty** (far steeper than the firmware `node distances` 10:20 implies;
the old all-192-core "5551 GB/s" was measuring cache, a now-fixed bug — real aggregate DRAM is
~762 GB/s). This is *why* crossing the complex hurts, and why NIC-local co-location wins.

### Placement experiment — node-split wins (run 5, MEASURED)

Run 5 built and swept the placement levers. At N=32 (IRQs NIC-local throughout):

| layout | Gbps | Gbps/core-equiv | receiver peak core | binding |
|---|---|---|---|---|
| `irqlocal_userN1` (all NIC-local — prior best) | 79.6 | 1.28 | 100% | CPU/crypto |
| `align` (per-flow IRQ↔app run-to-completion) | 79.3 | 1.29 | 100% | CPU/crypto |
| `rps_on` (RPS+RFS) | 79.0 | — | 100% | neutral, as predicted |
| **`split` (tunnels across BOTH NUMA nodes)** | **95.3** | **2.56** | **79%** | **unbound** |

Findings, each measured:
- **Node-split is the winner — 95.3 Gbps, within 5% of 100.** It nearly **doubles per-core
  efficiency** (2.56 vs 1.28) and the receiver is no longer pegged (79% peak core, 37
  core-equivalents vs 62). Cause: the all-NIC-local layout drove 62 busy core-equivalents
  through node 1's **single memory controller** — the receiver's hottest threads were
  `wg-crypt` kworkers *and* `mm_percpu_wq` (memory-management churn) at ~21% each. Spreading
  the tunnels across both nodes' memory subsystems relieves that contention. The cross-node
  cost is cheap (per-flow ~3 Gbps ≪ the 69 GB/s interconnect ceiling; single-access latency
  only 2.2×), so using all 192 cores beats hoarding the NIC's 96.
- This **contradicts the pre-run hypothesis** that node-split would be a dead end (the worry
  was node-0 cores reading packet payload across the slow link). The membw thread-sweep showed
  why the worry was misplaced: the link only saturates under *bulk* streaming, which this
  workload is nowhere near per flow.
- **`align` and `rps_on` were neutral** (~79, within noise). At N=32 the work is already spread
  across enough cores that exact per-flow IRQ↔app alignment adds nothing beyond node-locality,
  and RPS is pure overhead when flows are already HW-spread across 32 queues (as predicted).
- **wg-crypt workqueue is per-CPU** (`kworker/*-wg-crypt-wgN` exists but has no
  `/sys/devices/virtual/workqueue` entry → not sysfs-steerable); its placement follows the RX
  IRQ core, which is why IRQ pinning already captured it.

Caveat: the stage rollup reliably captures `decrypt` (the `wg-crypt` kworkers) but under-counts
`softirq`/`app` (the `napi/wg*` and `iperf3` threads show in the hot-threads list but fall
under the per-thread rollup threshold) — a `collect.sh` limitation to tighten next.

### Crossing 100 Gbps — node-split at higher N (run 6, MEASURED)

Run 5's split layout left the receiver at only 79% peak core — headroom. Run 6 raised N_MAX to
64 and swept the split layout further:

| N | Gbps | receiver core-equiv | receiver peak | binding |
|---|------|---------------------|---------------|---------|
| 32 | 99.3 | 65.5 | 97% | CPU/crypto |
| **40** | **103.2** | 70.3 | 100% | CPU/crypto |
| 48 | 100.3 | — | — | CPU/crypto |
| 64 | 103.1 | 89.5 | 100% | CPU/crypto |

**The fabric crosses 100 Gbps at N=40 (103.2) and holds ~100–103 through N=64.** Attribution is
clean: **no `bw_*`/`pps` allowance fired** at any point — the ~103 plateau is the aggregate
ChaCha20 decrypt wall with the receiver now genuinely CPU-saturated across both nodes (100% peak
core, 70–89 busy core-equivalents). Per-core efficiency is back to ~1.47 Gbps/core-equiv (vs
split's 2.56 at N=32) because at N≥40 we've gone from "relieving contention" to "saturating all
192 cores" — the real silicon ceiling for ChaCha20 at this MTU. With the `collect.sh` rollup
fixed, the stage breakdown is now complete (N=40 receiver: dec=22.1 sirq=1.6 ksd=1.4 mm=1.2
app=0.2 core-equiv) — decrypt remains dominant, as expected.

Note: the NIC exposes **32 HW RX queues**, so at N=64 two tunnels share each queue (2:1) — the
fabric scales fine past the queue count because the bottleneck is CPU, not queues.

### Beyond 103: bidirectional to ~136 Gbps, and where the real wall is (run 7)

103 Gbps was the *unidirectional* receive-CPU ceiling. To find the actual wall we (a) tried to
lower per-packet cost and (b) loaded both directions.

**Per-packet offloads — a dead end on ENA.** `nic-tune.sh probe` showed GSO/GRO already on, and
**`tx-udp-segmentation` / `tx-gso-partial` are `[fixed]` off** — the ENA driver doesn't expose
hardware UDP segmentation, so the crypto can't be batched that way. Enlarging the RX ring
1024→8192 (the one writable knob; TX is fixed at 1024) **did not help and slightly hurt**
(113→103 Gbps unidirectional at N=40) — predictable when the bottleneck is CPU, not buffering.
So per-byte cost is not reducible here; placement remains the only lever.

**Bidirectional — the path to maximum aggregate wire.** Running paired opposing flows (N tunnels
A→B *and* N tunnels B→A, on disjoint port blocks — *not* iperf3 `--bidir`, which would muddy
per-direction attribution and force both directions through one peer's serial crypto pipeline):

| N | A→B Gbps | B→A Gbps | aggregate wire | both nodes max-util |
|---|----------|----------|----------------|---------------------|
| **32** | 69.5 | 78.9 | **148.4** | 99% / 100% |
| 40 | 64.8 | 78.1 | 142.8 | 100% / 100% |
| 48 | 60.5 | 71.7 | 132.2 | 99% / 99% |
| 64 | 60.2 | 67.3 | 127.4 (oversubscribed: 128 flows) | 98% / 98% |

**Peak 148.4 Gbps aggregate wire** (clean symmetric `BIDIR=1` sweep), well past the 103
unidirectional. Attribution at the peak: **no allowance fired** (`bw_in`/`bw_out`/`pps` all 0 on
both nodes), and **both nodes CPU-saturated** (99–100%, ~68–71 core-equivalents each). The
`stage_crypt` breakdown confirms the mechanism: node A spends ~39 core-equiv in `wg-crypt`
(encrypting A→B *and* decrypting B→A simultaneously) — each node carries **two full crypto
pipelines**. So even full-duplex, the wall is **aggregate ChaCha20 CPU across 192 cores, never
the network.** This settles the "180 Gbps wall" question: ENA meters ingress and egress as
*independent* ~180 Gbps allowances (confirmed — neither `bw_in` nor `bw_out` ever fired at 148
aggregate), so there is **no single 180 aggregate cap**; the honest result is "per-direction
allowance never binds; CPU does." Throughput peaks at N=32 and tapers with N (full-duplex
oversubscription: 64 tunnels × 2 directions = 128 flows contending for the same cores).

### Still open: explicit cross-core stage pipelining

Node-split is run-to-completion *per node group*, not a true stage pipeline. A genuine pipeline
(decrypt on core X → socket delivery on Y → app on Z, via per-stage queues) might push further
now that we know moderate cross-node hand-offs are cheap — but the node-split result suggests
the bigger win was simply **relieving single-memory-controller contention**, which pipelining
within one node would *not* address. Untested; lower priority than it was pre-run.

### Run log

- **Run 1** (initial): placement + ena_express sweeps + NVMe→NVMe over nvme-tcp; surfaced six
  real-hardware harness bugs (now fixed). Established the ~60 Gbps unpinned plateau and that
  no AWS allowance binds.
- **Run 2** (instrumented): per-core histogram + `top_threads` + the pinning A/B. Resolved
  "how many cores / which threads" (≈57 core-equivalents across ~31 cores; napi + wg-crypt
  kworkers + ksoftirqd + iperf3) and demonstrated the **60 → 77 Gbps** pinning gain.
- **Run 3** (NUMA probe): classified the instance (2 nodes, NIC on node 1) and ran the
  userspace A/B — which *appeared* to favor the NIC-remote node, but exposed the NUMA-blind
  IRQ-pinning confound (IRQs were on the remote node).
- **Run 4** (NUMA, corrected): fixed IRQ pinning to NIC-local; the A/B flipped to confirm
  **all-NIC-local is best**. Measured per-NUMA memory bandwidth (381 local / 69 remote GB/s)
  and the NVMe near:far ratio (placement barely matters at this CPU-bound rate). A merge bug
  lost most of run 4's raw `datapoint.json` before commit; **run 4b re-ran the full experiment
  and captured it correctly** — the all-NIC-local layout reached **89.5 Gbps**.
- **Run 5** (placement levers, 2026-06-20): built+swept align / rps / node-split + a membw
  thread-sweep & latency probe. **Node-split reached 95.3 Gbps** (~2.56 Gbps/core-equiv) by
  spreading tunnels across both NUMA nodes to relieve single-memory-controller contention;
  settled that the 90% remote-BW figure is interconnect saturation (not per-access: 2.2×
  latency), and confirmed RPS neutral + wg-crypt WQ per-CPU.
- **Run 7** (push past 103, 2026-06-20): offload A/B (ENA UDP-GSO fixed-off, rings don't help)
  + first bidirectional node-split (~136 Gbps aggregate, ad-hoc reverse drive). Settled that
  there's no single 180 aggregate cap (in/out metered separately). Found+fixed the BIDIR
  sshd-MaxSessions bug.
- **Run 8** (tidy bidirectional, 2026-06-20): clean symmetric `BIDIR=1` sweep →
  **148.4 Gbps aggregate wire peak (N=32)**, balanced directions (69.5 + 78.9), both nodes
  CPU-saturated, no allowance fired. Full per-direction attribution captured. **Final result.**
- **Run 6** (push to 100, 2026-06-20): N_MAX=64 node-split sweep. **Crossed 100 Gbps at
  N=40 → 103.2 Gbps**, holding ~100–103 to N=64, no allowance fired — **goal met**. Also
  fixed the `collect.sh` stage rollup (was missing the `|__` pidstat prefix); attribution
  now complete.

## Reproducing

See `README.md` for the run order and `CLAUDE.md` for ground-truth facts and guardrails.
The harness is idempotent; `results/` → `report/` regenerates this document's tables, the
CSV, and the SVGs.
